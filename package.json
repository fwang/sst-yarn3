{
  "name": "sst-yarn3",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "sst test",
    "diff": "sst diff",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "eslintConfig": {
    "extends": [
      "serverless-stack"
    ]
  },
  "dependencies": {
    "sst": "2.0.22"
  }
}
